# 🧾 协议：Self-Contained Task 模板

**目的：** 用统一模板把任务“自包含化”，在执行前解决依赖与契约问题，减少执行期外部查询与上下文膨胀。

---

## 1. 任务元信息

- **标题**：
- **类型**：Feature / Bugfix / Refactor / Docs / Infra
- **优先级**：P0 / P1 / P2
- **范围边界（Scope）**：
  - **In-Scope**：
  - **Out-of-Scope**：

---

## 2. 背景与现状（只写必须信息）

- **现状描述**：
- **问题/机会**：
- **关键约束**：

---

## 3. 目标（可验证）

- **用户可见目标**：
- **工程目标**：

---

## 4. 依赖（必须在设计阶段解决）

按类型填写，并明确处理方式。

### 4.1 数据依赖

- **依赖数据**：
- **数据契约（字段/类型/约束）**：
- **来源**：真实 / mock / 先执行上游任务

### 4.2 接口依赖

- **调用接口**：
- **API Contract**：请求/响应 schema、错误码、边界条件
- **可用性**：真实 / mock server / stub

### 4.3 代码依赖

- **共享模块/工具**：
- **处理方式**：抽取到公共模块 / 复用现有 / 新增最小实现

### 4.4 知识依赖

- **业务背景要点**：
- **术语/规则**：

---

## 5. 确定性边界（Deterministic Interface）

- **输入**：
- **输出**：
- **不变量**：
- **失败模式**：

---

## 6. 交付物（产出清单）

- **文件变更列表（预期）**：
  - `path/to/file`：变更说明
- **新增文件列表（预期）**：
  - `path/to/new-file`：用途

---

## 7. 验收标准（必须可执行/可测试）

- **功能验收**：
- **回归范围**：
- **质量门禁**：
  - lint/类型检查：通过
  - 关键脚本：通过
  - 关键页面/流程：可验证

---

## 8. 执行说明（给 Worker 的最小输入包）

把 Worker 需要的一切信息收敛到此处（避免执行中再问）。

- **执行步骤（高层）**：
- **注意事项**：
- **禁止事项**：

